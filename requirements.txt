# Core data stack
pandas>=2.0
numpy>=1.24

# Econometrics (optional but recommended: enables full regression summaries;
# scripts fall back to a built-in numpy OLS if this is not installed)
statsmodels>=0.14

# Visualization (used in notebooks / reports)
matplotlib>=3.7
seaborn>=0.12

# Testing (optional)
pytest>=7.0
